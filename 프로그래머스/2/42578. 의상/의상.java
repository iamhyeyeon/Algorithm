import java.util.*;

class Solution {
    public int solution(String[][] clothes) {
        int answer = 0;
        Map<String, Integer> clotheMap = new HashMap<>();
        
        // 종류, 이름 hashMap 만들기 --> 이름은 알 필요가없지
        Arrays.stream()
            .forEach(clothe -> clotheMap
            .getOrDefault(clothe[1], 0)
        
        
            // System.out.println(clothe[0]);
            
        }
        
        return answer;
    }
}